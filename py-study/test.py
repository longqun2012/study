x=[1,2,3]
print(len(x))

#常用函数
#pow
#Python中的pow函数用于计算一个数的幂。‌它的基本语法是pow(x, y)，
# 其中x是基数，y是指数。pow函数还可以接受一个可选的第三个参数z，
# 表示取模（取余）操作

#len
#查看str或者列表的长度

#split
#Python 中的 split() 函数是一个非常实用的字符串方法，它用于将字符串分割成列表
# 。这个方法默认以空白字符（如空格、换行符\n、制表符\t等）作为分隔符，
# 但你也可以指定其他字符或字符串作为分隔符。

#replace
#python 中的 replace() 函数用于在字符串中查找并替换指定的子字符串，
# 其语法为：str.replace(old, new, count)。它可以替换所有匹配项（count=-1）
# ，或指定替换次数（count为正数）

l=['a','s','d','c']
for i,j in enumerate(l):
    print(i,j)
#enumerate